# Yours: your niche and settings (config.py.example is the template)
config.py

# Secrets: never commit these
.env
client_secret*.json
google-ads.yaml

# What the pipelines produce: runs, and cached API responses
runs/
cache/

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/

.DS_Store

# The explainer's build scripts: they use fonts and run data from this machine (docs/explainer.html is the self-contained source)
docs/build/
